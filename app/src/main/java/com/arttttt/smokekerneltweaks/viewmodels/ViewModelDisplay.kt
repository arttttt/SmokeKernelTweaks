package com.arttttt.smokekerneltweaks.viewmodels

import android.app.Application
import android.arch.lifecycle.AndroidViewModel

class ViewModelDisplay(application: Application) : AndroidViewModel(application) {
}