package com.arttttt.smokekerneltweaks.viewmodels

import android.app.Application
import android.arch.lifecycle.AndroidViewModel

class ViewModelCpu(application: Application) : AndroidViewModel(application) {
}